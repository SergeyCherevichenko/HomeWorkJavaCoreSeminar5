# Используем официальный образ OpenJDK с Java 11 в качестве базового образа
FROM openjdk:11

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# Копируем JAR-файл вашего приложения в контейнер
COPY target/ваш_файл.jar app.jar

# Запускаем ваше Java приложение при старте контейнера
CMD ["java", "-jar", "app.jar"]



