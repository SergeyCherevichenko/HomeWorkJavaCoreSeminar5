name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        run: |
          echo $DOCKER_PASSWORD | docker login -u cherevichenkosn@gmail.com --Bradley-10121981-stdin

      - name: Build and push Docker image
        run: |
          docker build -t yourdockerusername/dvwa-docker:latest .
          docker push yourdockerusername/dvwa-docker:latest

